void main() {
  for (var i = 0; i < 10; i++) {
    /* variable declaration and initialization is done at starting then condition is put on it
     and then increment is done */
    print('\$' * i);
  }
}
