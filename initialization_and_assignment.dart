void main() {
  String name = 'Sheep'; // Here a variable is declared and initialized
  print(name.toUpperCase());
  name = name
      .toLowerCase(); // Here the variable is reassigned. The = sign is the assignment operator
  print(name);
}
